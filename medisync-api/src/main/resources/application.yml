spring:
  datasource:
    url: jdbc:postgresql://10.23.240.60:5432/healthdb
    username: healthuser
    password: StrongP@ssw0rd
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        default_schema: dev
    show-sql: true

# JWT Configuration
jwt:
  secret: MediSync2026SecretKeyForJWTTokenGenerationAndValidation!@#$%^&*
  expiration: 86400000
